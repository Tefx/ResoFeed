# Protected Deviation Ledger — uiux-runtime-contract-retest-failure-retest

Successor closed: `uiux-runtime-contract-retest-failure-retest`

| Requirement ID / seam | Old assertion | New assertion | Authority citation | Why coverage is not weakened |
|---|---|---|---|---|
| OPML receipt / real-server-ui | Browser/API proof expected folder-oriented copy such as `folders flattened`. | Browser/API proof expects `OPML outlines flattened`. | `CONSTITUTION.md:60-61` forbids folders/tags/source category management; `CONSTITUTION.md:83-84` says OPML is source-subscription import only and not complete state restoration; `docs/PRD.md:73-78` says OPML folder structures are flattened while foldering/tagging must not exist for first value. | Import completion, source count, and real server setup remain asserted; only stale folder product terminology is removed. |
| DESIGN.FEED.NO_REPEATED_PREFIXES / Feed | Feed proof required visible `src: <source>` text in reader row. | Feed proof requires visible source value plus accessible `Source:` label and explicitly rejects visible `src:` reader-prefix text. | `docs/DESIGN.md:486-487`, `docs/DESIGN.md:528-530`, `docs/DESIGN.md:1033-1058`; `docs/audits/uiux-design-traceability-matrix.md:29`. | Provenance remains accessible and visible as a source value; coverage is stronger because it also catches forbidden prefix regressions. |
| DESIGN.FEED.NO_REPEATED_PREFIXES / Search | Search proof required visible `src: <source>` text in search result row. | Search proof requires visible source value plus accessible `Source:` label and explicitly rejects visible `src:` reader-prefix text. | `docs/DESIGN.md:486-487`, `docs/DESIGN.md:528-530`, `docs/DESIGN.md:1033-1058`; search reuses reader/feed anatomy in `docs/DESIGN.md:425-430` and traceability row `DESIGN.FEED.NO_REPEATED_PREFIXES`. | Search provenance remains tested through real browser result text and accessible label; forbidden reader prefix is now a negative assertion. |
| Language/Reprocess utility placement | Tests attempted to assert language/reprocess as persistent top chrome or closed-menu controls. | Tests open the `RESOFEED` surface menu and assert `LANG: EN` / `[REPROCESS LIBRARY]` only inside the opened utility menu. | `docs/DESIGN.md:426-429`, `docs/DESIGN.md:491-493`, `docs/DESIGN.md:522`, `docs/DESIGN.md:1016-1021`, `docs/DESIGN.md:1072-1078`. | Coverage now matches the low-chrome utility contract and adds negative proof that persistent top chrome is absent. |
| Inspector re-ingest model/prompt | Model/prompt controls could be interpreted as reusable preferences/defaults. | Proof requires item-scoped Inspector re-ingest and no durable prompt/model state after re-ingest. | `docs/ARCHITECTURE.md:27-29`; `docs/DESIGN.md:424`, `docs/DESIGN.md:1028-1029`, `docs/DESIGN.md:1063-1066`. | Behavior is more constrained: item-scoped positive path plus non-persistence negative proof. |

Result: all deviations are authority-cited and non-weakening; no product code, tests, requirements, or SHARP constraints were changed by this evidence-remediation step.
