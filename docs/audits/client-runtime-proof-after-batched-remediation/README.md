# Client Runtime Proof After Batched Remediation

Fresh runtime evidence captured on 2026-06-01 from isolated branch `vectl/step-client-runtime-proof-after-batched-remediation` at commit `fb369ff322daaba4d6e38dd872f9df5ceb2a35aa`.

Launch path used by `capture-runtime-proof.mjs`:

```bash
npm --prefix web run build
go build -o .test-artifacts/client-runtime-proof-after-batched-remediation/bin/resofeed ./cmd/resofeed
.test-artifacts/client-runtime-proof-after-batched-remediation/bin/resofeed serve --addr 127.0.0.1:<port> --public-url http://127.0.0.1:<port> --db .test-artifacts/client-runtime-proof-after-batched-remediation/fixtures/runtime-proof-<timestamp>.sqlite3 --owner-token <redacted>
```

Primary proof index: `manifest.json`.

Captured viewport sizes:

- Desktop: `1280x720`
- Mobile: `390x844`

Freshness note: these files were generated by the script in this directory after the upstream commits `batched-broad-e2e-ui-remediation` and `retest-green-runtime-ui-after-batched-remediation`; stale artifacts from the earlier `client-runtime-proof` step were not copied or reused.

Known non-blocking runtime anomaly: `network-log.json` records several `POST /api/steer/preview` HTTP 500 responses and matching generic browser console resource errors during deterministic command routing. The required surfaces still opened, rendered, and accepted interaction; API calls for source import, ingest, feed, item inspect, resonance, runtime language, source ledger, and operation snapshot returned 200.
