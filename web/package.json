{
  "name": "resofeed-web",
  "version": "0.2.11",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "lint": "npm run check",
    "test": "npm run check && npm run test:render",
    "test:render": "vitest run",
    "test:e2e": "playwright test --config ./playwright.config.ts",
    "test:e2e:ci-safe": "playwright test --config ./playwright.ci-safe.config.ts --project=chromium-ci-safe --retries=0",
    "test:e2e:smoke": "playwright test --config ./playwright.smoke.config.ts --project=chromium-ci-safe --retries=0",
    "test:e2e:runtime": "playwright test --config ./playwright.runtime.config.ts --project=chromium-ci-safe --retries=0",
    "test:e2e:live": "playwright test --config ./playwright.live.config.ts --project=live-openrouter --retries=0"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.8",
    "@sveltejs/kit": "^2.22.0",
    "@sveltejs/vite-plugin-svelte": "^5.1.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/svelte": "^5.3.1",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^25.6.2",
    "jsdom": "^29.1.1",
    "playwright": "^1.59.1",
    "svelte": "^5.33.0",
    "svelte-check": "^4.2.1",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^4.1.5"
  }
}
